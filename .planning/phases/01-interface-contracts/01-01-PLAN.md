---
phase: 01-interface-contracts
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - dashboard/schemas/dashboard-data.schema.json
  - dashboard/schema_validator.py
autonomous: true

must_haves:
  truths:
    - "Schema exists with required tool_type field using string enum"
    - "Schema validates timestamp, summary, metrics, data fields"
    - "Validator provides clear error messages with json_path for invalid fields"
    - "Validation can be imported and used by other modules"
  artifacts:
    - path: "dashboard/schemas/dashboard-data.schema.json"
      provides: "JSON Schema with Draft 2020-12 spec"
      contains: "tool_type.*enum"
    - path: "dashboard/schema_validator.py"
      provides: "DashboardDataValidator class"
      exports: ["DashboardDataValidator", "validate_dashboard_data"]
  key_links:
    - from: "dashboard/schema_validator.py"
      to: "dashboard/schemas/dashboard-data.schema.json"
      via: "load schema file"
      pattern: "open.*schema.*json"
---

<objective>
Define JSON Schema for dashboard-data.json with required tool_type discriminator field and create validation module.

Purpose: Establish the contract that all tools must follow, enabling explicit type-based routing in the dashboard instead of brittle heuristic detection.

Output:
- JSON Schema file with Draft 2020-12 specification
- Python validator module with clear error reporting
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-interface-contracts/01-RESEARCH.md

# Current heuristic detection to understand tool signatures
@dashboard/data_loader.py

# Sample outputs to understand current tool structures
# (Read one example from each tool during execution)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create JSON Schema Definition</name>
  <files>dashboard/schemas/dashboard-data.schema.json</files>
  <action>
Create the schema directory and JSON Schema file with:

1. Create `dashboard/schemas/` directory
2. Define schema with Draft 2020-12 specification:
   - `$schema`: "https://json-schema.org/draft/2020-12/schema"
   - `$id`: "https://airbais.com/schemas/dashboard-data/v1.0.0"
   - Required fields: tool_type, timestamp
   - Optional fields for backwards compatibility: summary, metrics, data (don't require yet)

3. Define tool_type as string enum with current tools:
   - intentcrawler
   - graspevaluator
   - llmevaluator
   - geoevaluator
   - rulesevaluator
   - llmstxtgenerator

4. Add descriptions to each field for documentation

5. Set `additionalProperties: true` to allow tool-specific fields

Note: Keep tool_type as the ONLY required field initially (plus timestamp). This allows backwards-compatible migration - tools can add tool_type without breaking existing files missing other fields.
  </action>
  <verify>
`python -c "import json; s=json.load(open('dashboard/schemas/dashboard-data.schema.json')); assert 'tool_type' in s.get('properties', {}); print('Schema valid')"` succeeds
  </verify>
  <done>
Schema file exists with tool_type enum containing all 6 tool identifiers, uses Draft 2020-12 spec
  </done>
</task>

<task type="auto">
  <name>Task 2: Create Schema Validator Module</name>
  <files>dashboard/schema_validator.py</files>
  <action>
Create Python validation module using jsonschema library:

1. Import jsonschema (Draft202012Validator), json, pathlib, logging

2. Create DashboardDataValidator class:
   - `__init__(self, schema_path: Path = None)`: Load schema from default path if not provided
   - `validate(self, data: dict) -> tuple[bool, list[str]]`: Return (is_valid, list of error messages)
   - `validate_file(self, filepath: Path) -> tuple[bool, list[str]]`: Convenience method for file validation

3. Error messages should include:
   - json_path (which field failed)
   - error message (what went wrong)
   - Format: "{json_path}: {message}"

4. Add convenience function `validate_dashboard_data(data: dict) -> tuple[bool, list[str]]` for simple imports

5. Use iter_errors() not validate() to collect ALL errors, not just first one

6. Handle missing schema file gracefully with clear error message

DO NOT: Add format validation yet (format_checker) - keep it simple. Format validation can be added later if needed.
  </action>
  <verify>
```bash
python -c "
from dashboard.schema_validator import DashboardDataValidator
v = DashboardDataValidator()
valid, errors = v.validate({'tool_type': 'graspevaluator', 'timestamp': '2025-01-23T10:00:00Z'})
assert valid, f'Valid data failed: {errors}'
valid2, errors2 = v.validate({'timestamp': '2025-01-23T10:00:00Z'})
assert not valid2, 'Missing tool_type should fail'
assert any('tool_type' in e for e in errors2), f'Error should mention tool_type: {errors2}'
print('Validator works correctly')
"
```
  </verify>
  <done>
Validator correctly validates tool_type presence and enum values, returns list of all errors with json_path
  </done>
</task>

<task type="auto">
  <name>Task 3: Add jsonschema to requirements</name>
  <files>requirements.txt</files>
  <action>
Add jsonschema dependency to requirements.txt if not already present:

1. Read current requirements.txt
2. Check if jsonschema is already listed
3. If not, add: `jsonschema>=4.20.0`

Note: Use >=4.20.0 not exact version - allows flexibility while ensuring Draft 2020-12 support.
  </action>
  <verify>
`grep -q "jsonschema" requirements.txt && echo "jsonschema found" || echo "MISSING"`
  </verify>
  <done>
jsonschema dependency is in requirements.txt
  </done>
</task>

</tasks>

<verification>
1. Schema file exists at dashboard/schemas/dashboard-data.schema.json
2. Schema contains tool_type with enum of 6 tools
3. Validator module can be imported: `from dashboard.schema_validator import DashboardDataValidator`
4. Validator correctly rejects data missing tool_type
5. Validator correctly rejects data with invalid tool_type value
6. Validator accepts valid data with correct tool_type
7. jsonschema is in requirements.txt
</verification>

<success_criteria>
- JSON Schema exists with all 6 tool types in enum
- DashboardDataValidator class validates against schema
- Invalid data produces error messages with field paths
- jsonschema dependency added to requirements
</success_criteria>

<output>
After completion, create `.planning/phases/01-interface-contracts/01-01-SUMMARY.md`
</output>
