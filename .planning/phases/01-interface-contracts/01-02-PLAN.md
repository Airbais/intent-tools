---
phase: 01-interface-contracts
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - intentcrawler/src/report_generator.py
  - graspevaluator/src/evaluator.py
  - llmevaluator/src/report_generator.py
  - geoevaluator/src/main.py
  - llmstxtgenerator/src/generator.py
  - rulesevaluator/src/output_generator.py
autonomous: true

must_haves:
  truths:
    - "All 6 tools include tool_type field in dashboard-data.json output"
    - "tool_type values match exact enum values from schema"
    - "Existing dashboard-data.json fields are preserved"
  artifacts:
    - path: "intentcrawler/src/report_generator.py"
      provides: "Dashboard data with tool_type"
      contains: "tool_type.*intentcrawler"
    - path: "graspevaluator/src/evaluator.py"
      provides: "Dashboard data with tool_type"
      contains: "tool_type.*graspevaluator"
    - path: "llmevaluator/src/report_generator.py"
      provides: "Dashboard data with tool_type"
      contains: "tool_type.*llmevaluator"
    - path: "geoevaluator/src/main.py"
      provides: "Dashboard data with tool_type"
      contains: "tool_type.*geoevaluator"
    - path: "llmstxtgenerator/src/generator.py"
      provides: "Dashboard data with tool_type"
      contains: "tool_type.*llmstxtgenerator"
    - path: "rulesevaluator/src/output_generator.py"
      provides: "Dashboard data with tool_type"
      contains: "tool_type.*rulesevaluator"
  key_links: []
---

<objective>
Add explicit tool_type field to all 6 tool output generators.

Purpose: Enable dashboard to identify tool type via explicit field instead of heuristic key detection, preventing misidentification and enabling schema validation.

Output: All tools generate dashboard-data.json with tool_type field matching schema enum values
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Tool output generators to modify
@intentcrawler/src/report_generator.py
@graspevaluator/src/evaluator.py
@llmevaluator/src/report_generator.py
@geoevaluator/src/main.py
@llmstxtgenerator/src/generator.py
@rulesevaluator/src/output_generator.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update IntentCrawler output</name>
  <files>intentcrawler/src/report_generator.py</files>
  <action>
Locate the `generate_dashboard_data` method and add tool_type field.

Find where dashboard_data dict is created (around line 73) and add:
```python
'tool_type': 'intentcrawler',
```

Place tool_type as the FIRST key in the dict for consistency and easy identification.

Preserve all existing fields: discovered_intents, by_section, total_pages_analyzed, etc.
  </action>
  <verify>
`grep -n "tool_type.*intentcrawler" intentcrawler/src/report_generator.py && echo "Found"`
  </verify>
  <done>
IntentCrawler output generator includes tool_type: 'intentcrawler' in dashboard data
  </done>
</task>

<task type="auto">
  <name>Task 2: Update GRASPEvaluator output</name>
  <files>graspevaluator/src/evaluator.py</files>
  <action>
Locate the `_prepare_dashboard_data` method (around line 298).

The current code already has "tool": "graspevaluator" - but we need "tool_type" to match schema exactly.

Option A (preferred): Add tool_type alongside existing tool field for backwards compatibility:
```python
"tool_type": "graspevaluator",
"tool": "graspevaluator",  # keep for backwards compat
```

Ensure tool_type is added at the beginning of the dict.
  </action>
  <verify>
`grep -n "tool_type.*graspevaluator" graspevaluator/src/evaluator.py && echo "Found"`
  </verify>
  <done>
GRASPEvaluator output generator includes tool_type: 'graspevaluator' in dashboard data
  </done>
</task>

<task type="auto">
  <name>Task 3: Update remaining 4 tools</name>
  <files>
llmevaluator/src/report_generator.py
geoevaluator/src/main.py
llmstxtgenerator/src/generator.py
rulesevaluator/src/output_generator.py
  </files>
  <action>
For each tool, locate the dashboard data generation method and add tool_type:

**LLMEvaluator** (src/report_generator.py):
- Two methods: `generate_dashboard_data` and `generate_multi_llm_dashboard_data`
- Add `'tool_type': 'llmevaluator'` to both methods
- Place at beginning of the returned dict

**GEOEvaluator** (src/main.py):
- Locate `_save_dashboard_data` method (around line 529)
- The dashboard_data dict is built - add tool_type
- Add `'tool_type': 'geoevaluator'` at start

**LLMSTxtGenerator** (src/generator.py):
- Locate `_generate_dashboard_data` method (around line 237)
- Add `'tool_type': 'llmstxtgenerator'` at start of dashboard_data dict

**RulesEvaluator** (src/output_generator.py):
- Locate `_generate_dashboard_data` method (around line 517)
- Current code already has "tool": "rulesevaluator"
- Add `'tool_type': 'rulesevaluator'` alongside existing tool field

For all tools: Preserve existing fields, only ADD tool_type.
  </action>
  <verify>
```bash
echo "Checking all tools..."
grep -l "tool_type" llmevaluator/src/report_generator.py && echo "llmevaluator: OK" || echo "llmevaluator: MISSING"
grep -l "tool_type.*geoevaluator" geoevaluator/src/main.py && echo "geoevaluator: OK" || echo "geoevaluator: MISSING"
grep -l "tool_type.*llmstxtgenerator" llmstxtgenerator/src/generator.py && echo "llmstxtgenerator: OK" || echo "llmstxtgenerator: MISSING"
grep -l "tool_type.*rulesevaluator" rulesevaluator/src/output_generator.py && echo "rulesevaluator: OK" || echo "rulesevaluator: MISSING"
```
  </verify>
  <done>
All 4 remaining tools include tool_type field matching their schema enum value
  </done>
</task>

</tasks>

<verification>
1. Each tool has tool_type in output generator:
   - `grep "tool_type" intentcrawler/src/report_generator.py`
   - `grep "tool_type" graspevaluator/src/evaluator.py`
   - `grep "tool_type" llmevaluator/src/report_generator.py`
   - `grep "tool_type" geoevaluator/src/main.py`
   - `grep "tool_type" llmstxtgenerator/src/generator.py`
   - `grep "tool_type" rulesevaluator/src/output_generator.py`

2. Tool type values are lowercase and match schema enum exactly:
   - intentcrawler, graspevaluator, llmevaluator, geoevaluator, llmstxtgenerator, rulesevaluator

3. Existing functionality preserved (no other changes to output structure)
</verification>

<success_criteria>
- All 6 tool output generators modified
- Each outputs tool_type matching schema enum value
- Existing output fields unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/01-interface-contracts/01-02-SUMMARY.md`
</output>
